for(var index in data){
    const brandOfCar = data[index].brandOfCar;
    const model = data[index].model;
    const plate = data[index].plate;
    const price = data[index].price;
    const numberOfPassenger = data[index].numberOfPassenger;
    const airConditioner = data[index].airConditioner;
    const consumption = data[index].consumption;
    const fuel = data[index].fuel;
    const gear = data[index].gear;
    const path = data[index].path;

    console.log(brandOfCar + model + plate + price);


  
    
    
    // let text = "<b>" + name + "</b>" + " (" + date + ") <br>";
    // text = text +  "Priority: " + priority + " <br>";
    // if(price >= 0){
    //     text = text + " Price: " + price + " <br>";
    // }
    // text = text + "Done: " + done;
    // if(done!=true){
    //     text = text + "<button onClick=\'complete(\""+id+"\")'\ id='cmpltbtn'>Complete</button>";
    // }
    
    // //var newElement = $("<div></div>").text(text);
    // //var elementBr = $("<br/>");

    // $("#mainContainer").append("<div class='container3'>" +"<div class='container4'>" + text + "</div>" + "</div>");
    // // if(done=="false"){
    // //     $(".container3").append("<button id='donebtn'>" + "Done" + "</buton>");
        
    // // }
}   

// airConditioner: true
// brandOfCar: "Mercedes"
// consumption: 9
// fuel: "50"
// gear: "7"
// model: "Eskovy"
// numberOfPassenger: 4
// path: ""
// plate: "KE487E1"
// price: 70